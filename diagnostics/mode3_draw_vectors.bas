10 REM ============================================================
20 REM MODE 3 DRAW vector string smoke test
30 REM Uses POINT() readback — no screenshot required
40 REM ============================================================
50 MODE 3
60 CLS
70 COLOR RED
80 DRAW "S4BM100,100R40D40L40U40"
90 IF POINT(140, 100) = RED THEN PRINT "TEST: DRAW_SQUARE = PASS" ELSE PRINT "TEST: DRAW_SQUARE = FAIL"
100 DRAW "S8BM200,200R1"
110 IF POINT(202, 200) = RED THEN PRINT "TEST: DRAW_SCALE = PASS" ELSE PRINT "TEST: DRAW_SCALE = FAIL"
120 COLOR BLUE
130 DRAW "BM300,300F10"
140 IF POINT(310, 310) = BLUE THEN PRINT "TEST: DRAW_DIAG = PASS" ELSE PRINT "TEST: DRAW_DIAG = FAIL"
150 DRAW "BM400,400M+20,+0"
160 IF POINT(420, 400) = BLUE THEN PRINT "TEST: DRAW_REL_M = PASS" ELSE PRINT "TEST: DRAW_REL_M = FAIL"
170 PRINT ""
180 PRINT "DRAW VECTOR TEST COMPLETE"
190 END